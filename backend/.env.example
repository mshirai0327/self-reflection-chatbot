# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Database connection
# Docker環境でのデフォルト設定です。必要に応じて変更してください。
DATABASE_URL="postgresql://user:password@db:5432/ai_reflection_db"
SHADOW_DATABASE_URL="postgresql://user:password@db:5432/ai_reflection_db_shadow"

# embedding用のAPIキー
# Google AI Studioで取得したAPIキーを設定してください。
GOOGLE_GENERATIVE_AI_API_KEY="YOUR_API_KEY_HERE"

# ChromaDB URL
# Docker環境でのサービス名を指定しています。
CHROMA_URL="http://chromadb:8000"

# 埋め込みモデルの変更（例: 最新版など）
GOOGLE_EMBEDDING_MODEL="gemini-embedding-001"
